{
  "model_name": "custom_cnn_baseline",
  "run_name": "custom_cnn_baseline_v2",
  "architecture": "CustomCNN(32,64,128,256)+AdaptiveAvgPool",
  "input_size": 224,
  "batch_size": 32,
  "epochs_max": 20,
  "optimizer": "AdamW",
  "learning_rate": 0.0003,
  "weight_decay": 0.0001,
  "loss": "CrossEntropyLoss",
  "scheduler": "ReduceLROnPlateau",
  "scheduler_factor": 0.5,
  "scheduler_patience": 2,
  "early_stopping_patience": 5,
  "dropout": 0.3,
  "seed": 20260902,
  "num_workers": 0,
  "pin_memory": false,
  "augmentation": "RandomHorizontalFlip(p=0.5); RandomRotation(degrees=10)",
  "normalization": "ImageNet",
  "experiment_name": "CastagNet_4_2",
  "max_estimated_training_hours": 2.0,
  "benchmark_train_batches": 12,
  "benchmark_validation_batches": 8
}
